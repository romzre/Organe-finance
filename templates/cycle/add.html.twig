{% extends 'partials/base_dashboard.html.twig' %}

{% block title %}Hello Dashboard
{% endblock %}

{% block body %}
	<div class="container">
		<div class="row">
			<h1 class="border-bottom border-warning">Bienvenue sur votre tableau de bord</h1>
		</div>
		{% for message in app.flashes('success') %}
				<div class="text-center alert-info alert col-8 col-md-6 col-xl-4 mx-auto my-5">{{message|raw}}</div>
			{% endfor %}
		<div class="row m-3">
			<h2>Cycle</h2>
		</div>
		<div class="row p-5">
			<div class="col-4 d-flex flex-column shadow justify-content-center border rounded-3 p-4">
				<div>
					<h3>Ajouter un cycle</h3>
				</div>
				{{form_start(CycleAddForm)}}
				<div class="form-group">
					{{ 
                        form_row(CycleAddForm.dateBegin, {'label' : 'Date de début', 'attr': {
                    'placeholder': 'ex : 1 janvier 2023' }})
																																																													}}
				</div>
				<div class="form-group">
					{{ 
                        form_row(CycleAddForm.dateEnd, {'label' : 'Date de fin', 'attr': {
                    'placeholder': 'ex : 31 janvier 2023' }})
																																																													}}
				</div>
				<div class="form-group">
					{{ 
                        form_row(CycleAddForm.solde, {'label' : 'Solde du compte', 'attr': {
                    'placeholder': 'ex : 330' }})
																																																													}}
				</div>
				<button type="submit" class="btn btn-primary">Enregistrer</button>
				{{form_end(CycleAddForm)}}
			</div>
		</div>
	</div>
</div>{% endblock %}
